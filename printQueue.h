#pragma once

#include <iostream>
#include <queue>
#include <string>
#include <map>

System::String^ printQueue(std::queue<int>);
System::String^ printMap(std::vector<std::pair<std::string, int>>);